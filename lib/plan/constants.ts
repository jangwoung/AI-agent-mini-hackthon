/**
 * Constants for schedule and reschedule (002-plan-schedule-reschedule).
 */

/** Maximum number of tasks per day when assigning or rescheduling. */
export const MAX_TASKS_PER_DAY = 2

/** Number of days to show in the schedule view (today + following days). */
export const SCHEDULE_VIEW_DAYS = 14
